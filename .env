# Use Vite dev proxy: requests go to /api → http://127.0.0.1:5000 (see vite.config.ts).
# Start backend first:  python api_server.py  (from repo root, with .venv active)
#
# Only set this if you need to override the default backend URL:
VITE_API_BASE_URL=https://ksl-be-ftj9.onrender.com

PASS=blyb rvmo ildq eqjm
EMAIL=mugishajm46@gmail.com
